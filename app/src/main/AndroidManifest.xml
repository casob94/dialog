<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uni.dialog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.RecordActivity">
            <intent-filter>
                <action android:name="android.intent.action.RECORD" />

                <category android:name="android.intent.category.INPUT" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.FactorActivity">
            <intent-filter>
                <action android:name="android.intent.action.FACTOR" />

                <category android:name="android.intent.category.INPUT" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.RecordEditActivity">
            <intent-filter>
                <action android:name="android.intent.action.EDITRECORD" />

                <category android:name="android.intent.category.EDIT" />
            </intent-filter>
        </activity>
    </application>

</manifest>